DEBUG=False
SECRET_KEY=change-me
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=postgres://postgres:postgres@localhost:5432/checktick
SECURE_SSL_REDIRECT=True # Set to False if not using HTTPS
CSRF_TRUSTED_ORIGINS=

# Self-hosted mode - gives all users Enterprise tier features
# Set to True if you're self-hosting and want all users to have Enterprise features
SELF_HOSTED=False

# Theme presets
# Branding and Theming Configuration
# These settings control the default branding and theming of the application
BRAND_TITLE=CheckTick
# Icon URLs - Leave empty to use the default inline SVG component (recommended)
# Set to a URL or static path (e.g., /static/icons/logo.svg) to use a custom icon
BRAND_ICON_URL=
BRAND_ICON_URL_DARK=
BRAND_ICON_ALT=CheckTick
BRAND_ICON_TITLE=CheckTick
BRAND_ICON_SIZE_CLASS=w-6 h-6
BRAND_ICON_SIZE=
BRAND_THEME=checktick-light
BRAND_THEME_PRESET_LIGHT=nord
BRAND_THEME_PRESET_DARK=business
BRAND_FONT_HEADING='IBM Plex Sans', ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, 'Apple Color Emoji', 'Segoe UI Emoji'
BRAND_FONT_BODY=Merriweather, ui-serif, Georgia, Cambria, 'Times New Roman', Times, serif
BRAND_FONT_CSS_URL=https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;600;700&family=Merriweather:wght@300;400;700&display=swap
BRAND_THEME_CSS_LIGHT=
BRAND_THEME_CSS_DARK=
# hCaptcha Configuration
# Get these from https://hcaptcha.com/
HCAPTCHA_SITEKEY=your_site_key
HCAPTCHA_SECRET=your_secret_key

# Email Configuration
# The email backend is automatically selected based on DEBUG setting:
#   - DEBUG=True (development): console backend (emails printed to terminal)
#   - DEBUG=False (production): SMTP backend (any SMTP provider)
# You can override this by setting EMAIL_BACKEND explicitly:
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# Email settings
DEFAULT_FROM_EMAIL=no-reply@yourdomain.com
SERVER_EMAIL=server@yourdomain.com

# SMTP Configuration (only needed in production when DEBUG=False)
# CheckTick works with ANY SMTP provider. Choose one and configure below:

# --- Option 1: Mailgun (Recommended) ---
# Get credentials from: https://app.mailgun.com/app/sending/domains
EMAIL_HOST=smtp.eu.mailgun.org
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=postmaster@mg.yourdomain.com
EMAIL_HOST_PASSWORD=your-mailgun-smtp-password

# --- Option 2: Gmail ---
# Note: Use App Password, not your regular Gmail password
# Generate at: https://myaccount.google.com/apppasswords
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=youremail@gmail.com
# EMAIL_HOST_PASSWORD=your-app-specific-password

# --- Option 3: SendGrid ---
# Get API key from: https://app.sendgrid.com/settings/api_keys
# Note: Username is literally "apikey"
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=your-sendgrid-api-key

# --- Option 4: Amazon SES ---
# Get SMTP credentials from AWS SES console
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-ses-smtp-username
# EMAIL_HOST_PASSWORD=your-ses-smtp-password

EMAIL_TIMEOUT=10

# External Dataset API (required)
# Used for fetching prefilled dropdown options (hospitals, NHS trusts, etc.)
# Get your free API key from: https://api.rcpch.ac.uk
EXTERNAL_DATASET_API_URL=https://api.rcpch.ac.uk
EXTERNAL_DATASET_API_KEY=your-rcpch-api-key-here

# Data Governance Configuration (Optional)
# These settings control data retention and export policies for GDPR/healthcare compliance
# Default values are sensible for most deployments, but can be customized per organisation

# Default retention period for survey data after closure (in months, range: 1-24)
# Default: 6 months
# CHECKTICK_DEFAULT_RETENTION_MONTHS=6

# Maximum retention period that can be set (in months)
# Default: 24 months (2 years)
# CHECKTICK_MAX_RETENTION_MONTHS=24

# Number of days before export download links expire
# Default: 7 days
# CHECKTICK_DOWNLOAD_LINK_EXPIRY_DAYS=7

# Days before deletion to send warning notifications (comma-separated list)
# Default: 30,7,1 (warnings at 1 month, 1 week, and 1 day before deletion)
# CHECKTICK_WARN_BEFORE_DELETION_DAYS=30,7,1

# OIDC Configuration
OIDC_RP_CLIENT_ID_AZURE=checktick-client-id
OIDC_RP_CLIENT_SECRET_AZURE=checktick-client-secret
OIDC_OP_TENANT_ID_AZURE=your-tenant-id

OIDC_RP_CLIENT_ID_GOOGLE = 'your-google-client-id'
OIDC_RP_CLIENT_SECRET_GOOGLE = 'your-google-client-secret'


# Redirect configuration
OIDC_RP_SIGN_ALGO = 'RS256'
OIDC_OP_JWKS_ENDPOINT_GOOGLE = 'https://www.googleapis.com/oauth2/v3/certs'
OIDC_OP_JWKS_ENDPOINT_AZURE = 'https://login.microsoftonline.com/common/discovery/v2.0/keys'

# Payment Processing
# The application automatically uses sandbox mode when DEBUG=True and production when DEBUG=False
# Get API keys from your payment provider's dashboard
PAYMENT_PROCESSING_SANDBOX_API_KEY=your-sandbox-api-key
PAYMENT_PROCESSING_PRODUCTION_API_KEY=your-production-api-key
PAYMENT_PROCESSING_SANDBOX_BASE_URL=
PAYMENT_PROCESSING_PRODUCTION_BASE_URL=
PAYMENT_PROCESSING_SANDBOX_WEBHOOK_SECRET=your-sandbox-webhook-secret
PAYMENT_PROCESSING_PRODUCTION_WEBHOOK_SECRET=your-production-webhook-secret

# Payment Client-Side Tokens (safe to expose in frontend)
# Get these from your payment provider's dashboard
PAYMENT_PROCESSING_SANDBOX_CLIENT_TOKEN=
PAYMENT_PROCESSING_PRODUCTION_CLIENT_TOKEN=

# Payment Price/Product IDs - Sandbox (for development/testing)
# Get these from your payment provider's dashboard
PAYMENT_PRICE_ID_PRO_SANDBOX=
PAYMENT_PRICE_ID_TEAM_SMALL_SANDBOX=
PAYMENT_PRICE_ID_TEAM_MEDIUM_SANDBOX=
PAYMENT_PRICE_ID_TEAM_LARGE_SANDBOX=
PAYMENT_PRICE_ID_ORGANIZATION_SANDBOX=
PAYMENT_PRICE_ID_ENTERPRISE_SANDBOX=

# Payment Price/Product IDs - Production
PAYMENT_PRICE_ID_PRO_PRODUCTION=
PAYMENT_PRICE_ID_TEAM_SMALL_PRODUCTION=
PAYMENT_PRICE_ID_TEAM_MEDIUM_PRODUCTION=
PAYMENT_PRICE_ID_TEAM_LARGE_PRODUCTION=
PAYMENT_PRICE_ID_ORGANIZATION_PRODUCTION=
PAYMENT_PRICE_ID_ENTERPRISE_PRODUCTION=

# IP Address Whitelist for payment webhooks (optional)
PAYMENT_PROCESSING_SANDBOX_IP_ADDRESSES_WHITELIST=
PAYMENT_PROCESSING_PRODUCTION_IP_ADDRESSES_WHITELIST=

# HashiCorp Vault Configuration (Required for hierarchical encryption)
# These credentials are generated when you run vault/setup_vault.py
# SECURITY: These values grant access to all encryption keys - store securely
VAULT_ADDR=https://vault.checktick.internal:8200
VAULT_ROLE_ID=your-vault-role-id-from-setup
VAULT_SECRET_ID=your-vault-secret-id-from-setup
# Platform custodian component (64-byte hex string)
# This is the offline component required to reconstruct platform master key
# CRITICAL: Store this value in multiple secure locations (not just .env)
PLATFORM_CUSTODIAN_COMPONENT=your-platform-custodian-component-hex
